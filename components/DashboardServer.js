// import { getFileAction } from "@/actions/getFileAction";
// import Dashboard from "./Dashboard";

// export default async function DashboardServer({ email, loadChat, selectedFileId, DeleteButton, setIsLogout }) {
//     const files = await getFileAction(email);
//     return (
//         <Dashboard files={files} loadChat={loadChat} selectedFileId={selectedFileId} DeleteButton={DeleteButton} setIsLogout={setIsLogout} />
//     )
// }